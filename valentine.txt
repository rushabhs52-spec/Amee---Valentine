<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amee, Will You Be My Valentine?</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
            font-family: 'Arial', sans-serif;
            overflow: hidden;
            position: relative;
        }
        h1 {
            font-size: 2.5em;
            color: #d63384;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 2em;
            animation: pulse 2s infinite;
            text-align: center;
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        .buttons {
            display: flex;
            gap: 2em;
        }
        button {
            padding: 1em 2em;
            font-size: 1.5em;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: absolute;
            transform: translate(-50%, -50%);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
        #yes {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            color: white;
            left: 50%;
            top: 70%;
        }
        #no {
            background: linear-gradient(45deg, #bdc3c7, #95a5a6);
            color: white;
            left: 30%;
            top: 70%;
        }
        button:hover {
            transform: translate(-50%, -50%) scale(1.1);
        }
        #success {
            display: none;
            text-align: center;
            font-size: 2.5em;
            color: #28a745;
            animation: bounce 1s infinite;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translate(-50%, -50%) translateY(0); }
            40% { transform: translate(-50%, -50%) translateY(-20px); }
            60% { transform: translate(-50%, -50%) translateY(-10px); }
        }
        .hearts {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 10;
        }
        .heart {
            position: absolute;
            font-size: 2em;
            animation: float 4s infinite linear;
            color: #ff69b4;
        }
        @keyframes float {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-100px) rotate(720deg); opacity: 0; }
        }
    </style>
</head>
<body>
    <h1>Amee, Will you go out with me on Valentine's Day? 💕</h1>
    <div class="buttons">
        <button id="yes">YES 😍</button>
        <button id="no">NO</button>
    </div>
    <div id="success">Wohoo!! Thank You for saying YES!! 🎉❤️</div>
    <div class="hearts" id="hearts"></div>

    <script>
        const noBtn = document.getElementById('no');
        const yesBtn = document.getElementById('yes');
        const successMsg = document.getElementById('success');
        const heartsContainer = document.getElementById('hearts');

        // NO button text cycle
        const noTexts = ['NO 😏', 'Are you sure?', 'Think again!', 'Pretty please?', 'NO WAY!'];

        let noClickCount = 0;

        // Move NO button + grow + change text
        noBtn.addEventListener('click', (e) => {
            e.preventDefault();
            noClickCount++;
            const maxX = window.innerWidth - 120;
            const maxY = window.innerHeight - 120;
            const newX = Math.random() * maxX;
            const newY = Math.random() * maxY;
            
            // Move
            noBtn.style.left = newX + 'px';
            noBtn.style.top = newY + 'px';
            
            // Grow size progressively
            const size = 1.2 + (noClickCount * 0.1);
            noBtn.style.fontSize = (1.5 + (noClickCount * 0.05)) + 'em';
            noBtn.style.padding = (1 + (noClickCount * 0.05)) + 'em ' + (2 + (noClickCount * 0.1)) + 'em';
            
            // Change text
            noBtn.textContent = noTexts[noClickCount % noTexts.length];
            
            // Shake animation
            noBtn.style.animation = 'shake 0.5s';
            setTimeout(() => { noBtn.style.animation = ''; }, 500);
        });

        // Add shake keyframes dynamically
        const style = document.createElement('style');
        style.textContent = `
            @keyframes shake {
                0%, 100% { transform: translate(-50%, -50%) rotate(0deg); }
                10%, 30%, 50%, 70%, 90% { transform: translate(-50%, -50%) rotate(-5deg); }
                20%, 40%, 60%, 80% { transform: translate(-50%, -50%) rotate(5deg); }
            }
        `;
        document.head.appendChild(style);

        // YES button: success + sound + hearts
        yesBtn.addEventListener('click', async () => {
            // Play celebratory sound (user-initiated click allows autoplay)
            try {
                const audioContext = new (window.AudioContext || window.webkitAudioContext)();
                const oscillator = audioContext.createOscillator();
                const gainNode = audioContext.createGain();
                oscillator.connect(gainNode);
                gainNode.connect(audioContext.destination);
                oscillator.frequency.setValueAtTime(523.25, audioContext.currentTime); // C5
                oscillator.frequency.linearRampToValueAtTime(659.25, audioContext.currentTime + 0.1); // E5
                oscillator.frequency.linearRampToValueAtTime(784.00, audioContext.currentTime + 0.2); // G5
                gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
                gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.5);
                oscillator.start(audioContext.currentTime);
                oscillator.stop(audioContext.currentTime + 0.5);
            } catch (err) {
                console.log('Sound play fallback (browser policy)');
            }

            // Hide elements, show success
            document.querySelector('h1').style.display = 'none';
            document.querySelector('.buttons').style.display = 'none';
            successMsg.style.display = 'block';
            
            // Hearts celebration
            createHearts();
        });

        // Floating hearts
        function createHearts() {
            for (let i = 0; i < 60; i++) {
                setTimeout(() => {
                    const heart = document.createElement('div');
                    heart.innerHTML = ['❤️', '💕', '💖', '✨'][Math.floor(Math.random() * 4)];
                    heart.className = 'heart';
                    heart.style.left = Math.random() * 100 + '%';
                    heart.style.animationDuration = (3 + Math.random() * 2) + 's';
                    heart.style.animationDelay = Math.random() * 1 + 's';
                    heartsContainer.appendChild(heart);
                    setTimeout(() => heart.remove(), 5000);
                }, i * 80);
            }
        }
    </script>
</body>
</html>
